import React, { useEffect, useState } from "react";

const SearchResultsListItem = ({line, stops, distance}) => {
	console.log(line, stops, distance);
	return (
		<div>ListItem</div>
	);
};

export default SearchResultsListItem;